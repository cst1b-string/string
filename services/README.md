# `services/`

This directory contains services, which are backend applications that provide an API to frontend applications.

Services can be written in any language, but the mono repo is configured to support Node.js and TypeScript, as well as Rust.
